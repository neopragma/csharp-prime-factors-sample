using System;
using System.Collections.Generic;

namespace PrimeFactors
{
    public static class PrimeFactors
    {

    // We know we want to call the method 'Of'.
    // We know the method will take a single integer argument.
    // We know the method will return something, but we haven't decided what.
    // We start by returning null, and we'll see where the tests lead us from there.
    // To make the code compile, we used Transformation #1: ({}->nil)
        public static List<int> Of(int value) {
            return null;
        }
    }
}
